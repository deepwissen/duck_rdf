# name: test/sql/pivot_rdf.test
# description: test pivot_rdf extension function — minimal bisect
# group: [sql]

require rdf

# Test 1: error handling (bind only, throws before profiling)
statement error
SELECT * FROM pivot_rdf('not_exist.nt');
----
IO Error: No files found matching: not_exist.nt

# Test 2: bind + scan with COUNT (no DESCRIBE)
query I
SELECT COUNT(*) FROM pivot_rdf('test/rdf/tests.nt');
----
4
